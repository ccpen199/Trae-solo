export enum VendorStatus {
  ACTIVE = 'active',
  PENDING = 'pending',
  BLOCKED = 'blocked',
  SUSPENDED = 'suspended',
}

export enum VendorAuthType {
  API_KEY = 'api_key',
  OAUTH2 = 'oauth2',
  CERTIFICATE = 'certificate',
  TOKEN = 'token',
}

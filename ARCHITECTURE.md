# 校园直饮水IoT服务平台 - 系统架构设计

## 一、整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              客户端层                                    │
├────────────────┬──────────────────┬──────────────────┬──────────────────┤
│  学生小程序    │  投资商Web后台   │  运维Web后台     │   IoT设备端      │
│  (Taro)        │  (React)         │  (React)         │   (ESP32/固件)   │
│  - 扫码启动    │  - 设备集群地图  │  - 远程重启      │   - 数据采集     │
│  - 蓝牙连接    │  - 能耗曲线      │  - 参数下发      │   - 指令执行     │
│  - 实时扣费    │  - ROI仪表盘     │  - 固件升级      │   - 离线缓存     │
│  - 账单查询    │  - 故障预警      │  - 故障处理      │   - 断网续传     │
└────────┬───────┴──────────────────┴──────────────────┴────────┬─────────┘
         │                                                      │
         │ HTTPS / WSS                                           │ MQTT / BLE
         │                                                      │
┌────────▼──────────────────────────────────────────────────────▼─────────┐
│                              接入层                                     │
├──────────────────────────────┬──────────────────────────────────────────┤
│  API Gateway (RESTful)       │  MQTT Broker (EMQX 5.x)                  │
│  - 身份认证/鉴权             │  - MQTT 3.1.1 / 5.0                      │
│  - 流量控制/限流             │  - WebSocket MQTT                        │
│  - 日志审计                  │  - 设备证书认证                          │
│  - 跨域处理                  │  - 主题权限控制                          │
└────────┬─────────────────────┴──────────────────┬───────────────────────┘
         │                                         │
         │                                         │
┌────────▼─────────────────────────────────────────▼───────────────────────┐
│                              业务服务层                                  │
├────────────────┬────────────────┬────────────────┬──────────────────────┤
│  用户服务      │  设备服务      │  交易服务      │  IoT数据服务          │
│  - 注册登录    │  - 设备管理    │  - 实时扣费    │  - 数据采集          │
│  - 权限管理    │  - 绑定解绑    │  - 账单生成    │  - 指令下发          │
│  - 账户体系    │  - 状态监控    │  - 支付集成    │  - 离线数据同步      │
│  - 角色管理    │  - 固件管理    │  - 钱包管理    │  - 规则引擎          │
├────────────────┼────────────────┼────────────────┼──────────────────────┤
│  运维服务      │  报表服务      │  支付服务      │  安全服务             │
│  - 远程控制    │  - 统计分析    │  - 支付宝H5    │  - 双向认证          │
│  - 参数配置    │  - 能耗报表    │  - 订单管理    │  - AES加密           │
│  - 固件升级    │  - ROI计算     │  - 退款处理    │  - 防重放攻击        │
│  - 工单系统    │  - 地图可视化  │                │  - 密钥管理          │
└────────┬───────┴────────────────┴────────────────┴──────────┬───────────┘
         │                                                    │
         │                                                    │
┌────────▼────────────────────────────────────────────────────▼───────────┐
│                              数据存储层                                  │
├──────────────────────────────┬──────────────────────────────────────────┤
│  MongoDB (主数据库)          │  Redis (缓存/消息队列)                   │
│  - 用户/设备/交易数据        │  - 会话缓存                              │
│  - IoT时序数据               │  - 实时数据缓存                          │
│  - 配置/日志数据             │  - 消息队列(指令/升级)                   │
│  - 报表统计数据              │  - 分布式锁                              │
└──────────────────────────────┴──────────────────────────────────────────┘
```

## 二、技术选型

### 2.1 后端服务
| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | 18+ | 服务端运行时 |
| Express.js | 4.18+ | Web框架 |
| Mongoose | 7.0+ | MongoDB ODM |
| MQTT.js | 5.0+ | MQTT客户端 |
| Socket.IO | 4.6+ | 实时通信 |
| JOI | 17.0+ | 参数校验 |
| jsonwebtoken | 9.0+ | JWT认证 |
| bcryptjs | 2.4+ | 密码加密 |
| crypto-js | 4.2+ | AES加密 |
| ioredis | 5.3+ | Redis客户端 |
| dayjs | 1.11+ | 日期处理 |
| winston | 3.11+ | 日志系统 |

### 2.2 前端 - 投资商/运维后台
| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.2+ | 前端框架 |
| TypeScript | 5.0+ | 类型安全 |
| Vite | 5.0+ | 构建工具 |
| Ant Design | 5.12+ | UI组件库 |
| ECharts | 5.4+ | 图表可视化 |
| @ant-design/charts | 2.0+ | 专业图表 |
| react-router-dom | 6.20+ | 路由管理 |
| zustand | 4.4+ | 状态管理 |
| axios | 1.6+ | HTTP客户端 |
| socket.io-client | 4.6+ | 实时通信 |
| leaflet | 1.9+ | 地图可视化 |

### 2.3 学生端小程序
| 技术 | 版本 | 用途 |
|------|------|------|
| Taro | 3.6+ | 跨端小程序框架 |
| React | 18.2+ | 前端框架 |
| TypeScript | 5.0+ | 类型安全 |
| @tarojs/components | 3.6+ | 小程序组件 |
| @tarojs/taro | 3.6+ | Taro API |

### 2.4 IoT设备通信
| 技术 | 版本 | 用途 |
|------|------|------|
| EMQX | 5.3+ | MQTT Broker |
| MQTT 5.0 | - | 通信协议 |
| BLE 5.0 | - | 近场通信 |
| mbedtls | 3.4+ | TLS加密 |

## 三、核心业务流程

### 3.1 学生取水流程
```
1. 学生打开小程序 → 输入手机号 → 验证码登录
2. 扫码设备二维码 / 蓝牙搜索设备
3. 设备双向认证 → 建立安全连接
4. 选择水温 → 点击启动
5. 设备出水 → 实时流量/水温数据上报
6. 实时扣费（按升计价）→ 余额不足自动唤起支付宝H5
7. 停止出水 → 生成电子账单（水温、用量、金额）
8. 账单推送至小程序
```

### 3.2 支付流程
```
1. 余额检测 < 阈值 → 唤起充值页面
2. 选择充值金额 → 提交订单
3. 后端调用支付宝H5支付接口 → 返回支付链接
4. 小程序跳转支付宝H5页面
5. 用户完成支付 → 支付宝异步通知
6. 后端验签 → 更新账户余额 → 发送充值成功通知
```

### 3.3 设备离线重连流程
```
1. 设备检测网络断开 → 启动离线缓存模式
2. 交易数据加密存储到设备本地Flash
3. 网络恢复 → 设备主动重连MQTT Broker
4. 批量上报离线数据（带时间戳）
5. 后端去重处理 → 补扣费用 / 同步状态
6. 设备清除已同步缓存
```

## 四、安全机制

### 4.1 设备双向认证
```
设备端                                    服务端
   │                                        │
   ├─── 连接请求(deviceId, random1) ───────▶│
   │                                        │
   │◀── 响应(random2, sign(random1)) ───────┤
   │                                        │  校验签名
   ├─── sign(random2, deviceSecret) ───────▶│
   │                                        │
   │◀────── 认证结果 ───────────────────────┤
```

### 4.2 交易数据加密
- **算法**: AES-256-CBC
- **密钥**: 设备唯一密钥（出厂预置 + 动态更新）
- **数据**: 每笔交易流水加密后存储和传输

### 4.3 防重放攻击
- 每个请求携带唯一nonce + 时间戳
- nonce缓存5分钟，重复请求直接拒绝
- 时间戳偏差超过300秒拒绝

## 五、项目目录结构

```
campus-water-iot-platform/
├── backend/                     # 后端服务
│   ├── src/
│   │   ├── controllers/         # 控制器
│   │   ├── models/              # 数据模型
│   │   ├── routes/              # 路由
│   │   ├── services/            # 业务服务
│   │   ├── middleware/          # 中间件
│   │   ├── utils/               # 工具函数
│   │   ├── iot/                 # IoT通信模块
│   │   ├── security/            # 安全模块
│   │   └── config/              # 配置
│   ├── tests/                   # 测试
│   └── Dockerfile
│
├── admin/
│   ├── investor/                # 投资商后台（React）
│   │   ├── src/
│   │   │   ├── pages/
│   │   │   ├── components/
│   │   │   ├── services/
│   │   │   └── store/
│   │   └── Dockerfile
│   │
│   └── operator/                # 运维后台（React）
│       ├── src/
│       │   ├── pages/
│       │   ├── components/
│       │   ├── services/
│       │   └── store/
│       └── Dockerfile
│
├── miniprogram/                 # 学生端小程序（Taro）
│   ├── src/
│   │   ├── pages/
│   │   ├── components/
│   │   ├── services/
│   │   └── utils/
│   └── project.config.json
│
├── firmware/                    # 设备固件（示例）
├── docs/                        # 文档
├── data/                        # 数据挂载目录
├── uploads/                     # 文件上传目录
├── docker-compose.yml
├── package.json
├── .env.example
└── ARCHITECTURE.md
```

## 六、关键设计决策

### 6.1 为什么选择 MQTT 而非 HTTP？
- 轻量级协议，适合低带宽、不稳定网络环境
- 双向通信，支持服务端主动下发指令
- QoS机制保障消息可靠传输
- 设备离线消息缓存

### 6.2 为什么选择 MongoDB 存储时序数据？
- 灵活的文档结构适配不同设备型号
- TTL索引自动清理历史数据
- 聚合管道支持复杂统计分析
- 水平扩展能力强

### 6.3 为什么使用 Taro 开发小程序？
- 一套代码多端运行（微信、支付宝小程序）
- React 开发体验，降低学习成本
- 丰富的生态和组件库

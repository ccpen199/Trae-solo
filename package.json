{
  "name": "neighborhood-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w @neighborhood/backend",
    "dev:frontend": "npm run dev -w @neighborhood/frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "npm run build -w @neighborhood/shared && npm run build -w @neighborhood/backend && npm run build -w @neighborhood/frontend",
    "lint": "npm run lint -w @neighborhood/backend && npm run lint -w @neighborhood/frontend",
    "test": "npm run test -w @neighborhood/backend",
    "db:generate": "npm run db:generate -w @neighborhood/backend",
    "db:migrate": "npm run db:migrate -w @neighborhood/backend",
    "db:seed": "npm run db:seed -w @neighborhood/backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}

{
  "name": "home-decoration-saas",
  "version": "1.0.0",
  "description": "家装产业协同SaaS平台",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm --prefix frontend install && npm --prefix backend install",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "start:frontend": "nohup npm --prefix frontend run dev > frontend.log 2>&1 &",
    "start:backend": "nohup npm --prefix backend run dev > backend.log 2>&1 &",
    "start": "npm run start:backend && sleep 3 && npm run start:frontend",
    "stop": "node scripts/stop.js",
    "check": "node scripts/check.js"
  },
  "keywords": ["saas", "home-decoration", "decoration"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

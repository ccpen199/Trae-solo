# 个人年度目标复盘系统 - 测试验收文档

## 📋 项目概述

个人年度目标复盘工具，支持目标计划、执行记录和年度复盘全流程管理。

### 技术栈
- **前端**: Vue 3 + Vite + Element Plus + Pinia + ECharts
- **后端**: Node.js + Express + better-sqlite3 + JWT
- **数据库**: SQLite (data/app.sqlite)

---

## 🌐 端口配置

| 服务 | 端口 | 地址 |
|------|------|------|
| 前端 | 44488 | http://127.0.0.1:44488 |
| 后端 | 54488 | http://127.0.0.1:54488/api |

> 端口公式: 目录名 may-63488，后四位 3488
> - 默认: 前端 40000+3488=43488, 后端 50000+3488=53488
> - 备用槽位1: 前端 41000+3488=44488, 后端 51000+3488=54488 (当前使用)
> - 备用槽位2-5: 42000/52000, 43000/53000, 44000/54000, 45000/55000 + 3488

---

## 🚀 启动与停止命令

### 启动服务
```bash
# 启动后端
./start-backend.sh

# 启动前端
./start-frontend.sh
```

### 检查服务状态
```bash
PROJECT_DIR="$(pwd)"
FRONTEND_PORT=44488
BACKEND_PORT=54488
frontend_pid=$(lsof -nP -iTCP:$FRONTEND_PORT -sTCP:LISTEN -t | head -n1)
backend_pid=$(lsof -nP -iTCP:$BACKEND_PORT -sTCP:LISTEN -t | head -n1)
ps -p "$frontend_pid" -o pid=,ppid=,stat=,cwd=,command=
ps -p "$backend_pid" -o pid=,ppid=,stat=,cwd=,command=
curl -I --max-time 5 http://127.0.0.1:$FRONTEND_PORT/
curl -sS --max-time 5 http://127.0.0.1:$BACKEND_PORT/api/health
```

### 停止服务（仅终止本项目进程）
```bash
# 停止前端
PORT=44488
PROJECT_DIR="/Users/chen/Documents/trae_projects/local_projects/may-63488"
pid=$(lsof -nP -iTCP:$PORT -sTCP:LISTEN -t | head -n1)
cwd=$(lsof -p "$pid" -d cwd -Fn | grep '^n' | cut -c2- | grep "$PROJECT_DIR" | head -1)
cmd=$(ps -o command= -p "$pid")
if [[ "$cwd" == "$PROJECT_DIR"* ]] || [[ "$cmd" == *"$PROJECT_DIR"* ]]; then
  kill "$pid"
fi

# 停止后端
PORT=54488
pid=$(lsof -nP -iTCP:$PORT -sTCP:LISTEN -t | head -n1)
cmd=$(ps -o command= -p "$pid")
if [[ "$cmd" == *"$PROJECT_DIR"* ]]; then
  kill "$pid"
fi
```

---

## 📁 主要文件结构

```
may-63488/
├── .env                          # 环境变量配置
├── data/
│   └── app.sqlite               # SQLite 数据库
├── backend/
│   ├── package.json
│   └── src/
│       ├── server.js            # 主服务入口
│       ├── routes/
│       │   ├── auth.js          # 认证接口
│       │   ├── goals.js         # 目标管理接口
│       │   ├── execution.js     # 执行记录接口
│       │   ├── deviation.js     # 偏差分析接口
│       │   ├── export.js        # 导出报告接口
│       │   └── admin.js         # 管理后台接口
│       ├── middleware/
│       │   └── auth.js          # JWT 认证中间件
│       ├── utils/
│       │   ├── db.js            # 数据库连接
│       │   └── progress.js      # 进度计算工具
│       └── scripts/
│           └── initDb.js        # 数据库初始化脚本
├── frontend/
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── router/index.js
│       ├── stores/user.js
│       ├── api/
│       │   ├── request.js
│       │   └── index.js
│       ├── styles/index.css
│       └── views/
│           ├── Login.vue
│           ├── Layout.vue
│           ├── Dashboard.vue
│           ├── Goals.vue
│           ├── GoalDetail.vue
│           ├── Execution.vue
│           ├── Habits.vue
│           ├── Deviation.vue
│           ├── AnnualReview.vue
│           ├── Export.vue
│           └── Admin.vue
├── start-frontend.sh
├── start-backend.sh
├── frontend.log
├── backend.log
└── TESTING.md
```

---

## 🧪 测试样例

### 🔹 第一类：正常场景样例

#### 样例 1: 用户登录与目标浏览
**测试路径**:
1. 打开 http://127.0.0.1:44488
2. 输入账号 `demo` / 密码 `user123` 登录
3. 进入仪表盘，查看年度概览
4. 进入目标库，浏览5个维度目标
5. 点击"晋升高级工程师"目标查看详情

**预期结果**:
- 登录成功，跳转仪表盘
- 仪表盘显示: 5个目标、平均进度、维度分布图
- 目标库显示5张目标卡片（事业、健康、财务、学习、关系）
- 目标详情显示3个关键结果、进度条、里程碑时间线

**接口验证**:
```bash
# 登录
curl -X POST http://127.0.0.1:54488/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"demo","password":"user123"}'

# 获取目标列表
curl http://127.0.0.1:54488/api/goals?year=2026 \
  -H "Authorization: Bearer <token>"
```

---

#### 样例 2: 创建新目标与关键结果
**测试路径**:
1. 目标库页面点击「新增目标」
2. 填写表单:
   - 年份: 2026
   - 维度: 学习
   - 标题: "学习Python编程"
   - 权重: 1.0
   - 截止日期: 2026-12-31
3. 提交后进入目标详情
4. 添加关键结果:
   - 标题: "完成Python基础课程"
   - 目标值: 1
   - 单位: 门
5. 添加里程碑: "完成第一阶段" / 2026-06-30

**预期结果**:
- 目标创建成功，出现在目标库
- 关键结果添加成功，进度0%
- 里程碑显示在时间线上

**接口验证**:
```bash
# 创建目标
curl -X POST http://127.0.0.1:54488/api/goals \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"year":2026,"dimension":"学习","title":"学习Python编程","weight":1}'
```

---

#### 样例 3: 周计划与任务管理
**测试路径**:
1. 进入「周计划」页面
2. 点击「新增任务」
3. 填写:
   - 标题: "完成Python第三章练习"
   - 优先级: 高
   - 关联关键结果: "完成Python基础课程"
4. 任务列表中点击「开始」→ 状态变为进行中
5. 点击「完成」→ 状态变为已完成

**预期结果**:
- 任务创建成功，显示在「待办」列
- 状态切换正常，三列布局正确更新
- 关键结果进度自动更新

---

#### 样例 4: 习惯打卡
**测试路径**:
1. 进入「习惯打卡」页面
2. 点击「新增习惯」
3. 填写:
   - 名称: "每日Python学习"
   - 频率: 每日
   - 目标次数: 1
4. 在习惯卡片上点击「打卡」
5. 查看30天热力图

**预期结果**:
- 习惯创建成功，连续打卡天数显示1
- 打卡成功，显示成功动画
- 热力图当日格子变色

---

#### 样例 5: 偏差分析记录
**测试路径**:
1. 进入「偏差分析」页面
2. 点击「新增记录」
3. 填写:
   - 关键结果: "体重维持在70kg以内"
   - 类型: 延期
   - 原因: "近期聚餐较多，饮食不规律"
   - 解决方案: "下周开始控制晚餐热量"
4. 保存后查看列表

**预期结果**:
- 偏差记录创建成功
- 按类型统计卡片数量更新
- 列表显示关联的目标和关键结果

---

#### 样例 6: 年度复盘与导出
**测试路径**:
1. 进入「年度复盘」页面
2. 查看年度数据概览
3. 在「总结」文本域填写: "2026年是成长的一年..."
4. 等待2秒自动保存
5. 进入「导出报告」页面
6. 开启「隐私模式」，点击「导出JSON」

**预期结果**:
- 复盘编辑器自动保存，状态显示"已保存"
- 报告预览显示年度总结、目标详情、时间线、行动清单
- 隐私模式下敏感信息显示为"[隐私保护]"
- JSON文件下载成功

---

### 🔹 第二类：边界场景样例

#### 样例 7: 目标进度自动计算
**测试路径**:
1. 进入目标详情页，关键结果"跑步累计200公里"
2. 添加执行记录:
   - 日期: 今日
   - 进度增量: 5
   - 描述: "晨跑5公里"
   - 投入时间: 40
3. 查看关键结果进度变化
4. 再添加2条执行记录，观察进度是否超过100%

**预期结果**:
- 关键进度自动更新 (当前值从45→50，进度从X→Y)
- 进度最高为100%，不会超出
- 目标总进度同步更新
- 执行记录列表显示新增记录

---

#### 样例 8: 空数据与异常值处理
**测试路径**:
1. 创建无关键结果的目标
2. 查看该目标的进度显示
3. 在周计划页面快速切换上一周/下一周
4. 查看没有数据的周是否自动创建空计划

**预期结果**:
- 无KR的目标进度显示0%
- 周切换流畅，不存在的周自动创建
- 空状态显示友好提示

---

#### 样例 9: 管理员权限
**测试路径**:
1. 退出登录，使用 `admin` / `admin123` 登录
2. 进入「管理后台」
3. 查看统计卡片、用户列表、操作日志
4. 将某个普通用户设置为禁用
5. 尝试用被禁用账号登录

**预期结果**:
- 管理后台页面正常显示
- 用户状态切换成功
- 被禁用账号无法登录，提示"用户名或密码错误"

---

### 🔹 第三类：冲突场景样例

#### 样例 10: 重复打卡冲突
**测试路径**:
1. 习惯打卡页面，对同一个习惯点击打卡
2. 立即再次点击打卡

**预期结果**:
- 第一次打卡成功
- 第二次打卡失败，提示"今日已打卡"
- 打卡次数不会重复增加

---

#### 样例 11: 端口占用冲突
**测试路径**:
1. 手动占用44488端口（可选测试）
2. 重启前端服务

**预期结果**:
- 服务自动检测端口占用
- 尝试使用下一个备用槽位（45488/55488）
- 自动更新.env文件
- 若所有槽位占用，启动失败并打印提示

---

### 🔹 第四类：失败场景样例

#### 样例 12: 认证失败
**测试路径**:
1. 登录页面输入错误密码
2. 访问需要登录的页面（直接访问URL）
3. 使用过期的token调用API

**预期结果**:
- 错误密码提示"用户名或密码错误"
- 未登录自动跳转登录页
- 过期token返回401，自动跳转登录

---

#### 样例 13: 参数校验失败
**测试路径**:
1. 创建目标时不填写标题
2. 创建关键结果时目标值输入负数
3. 注册时两次密码不一致

**预期结果**:
- 表单显示对应错误提示
- 表单无法提交
- 后端返回400错误和明确的错误信息

---

#### 样例 14: 越权访问
**测试路径**:
1. 使用demo用户登录
2. 尝试调用管理后台API
3. 尝试访问其他用户的目标数据（通过构造URL或API参数）

**预期结果**:
- 管理后台页面不显示在菜单
- 访问/admin自动跳转首页
- 越权API返回403错误

---

## ✅ 导出核对清单

### 年度报告导出验证
| 检查项 | 状态 | 说明 |
|--------|------|------|
| 年度总结数据完整 | ✅ | 包含目标数、完成数、平均进度等 |
| 目标详情导出 | ✅ | 含有关键结果、里程碑、偏差记录 |
| 时间线生成正确 | ✅ | 按时间顺序排列里程碑、执行、偏差事件 |
| 行动清单生成 | ✅ | 包含未完成KR、逾期里程碑、未解决偏差 |
| 图表数据完整 | ✅ | 月度时间柱状图、习惯统计数据 |
| 隐私模式生效 | ✅ | 敏感字段替换为"[隐私保护]" |
| 历史年度对比 | ✅ | 支持多年份数据对比 |
| JSON格式正确 | ✅ | 可解析、无乱码 |

### 数据库落库验证
| 检查项 | 状态 | SQL查询 |
|--------|------|----------|
| 用户数据落库 | ✅ | `SELECT * FROM users;` |
| 目标数据落库 | ✅ | `SELECT * FROM goals;` |
| 关键结果落库 | ✅ | `SELECT * FROM key_results;` |
| 执行记录落库 | ✅ | `SELECT * FROM execution_records;` |
| 偏差分析落库 | ✅ | `SELECT * FROM deviation_analyses;` |
| 习惯打卡落库 | ✅ | `SELECT * FROM habit_checkins;` |
| 操作日志落库 | ✅ | `SELECT * FROM operation_logs ORDER BY id DESC LIMIT 10;` |
| 年度复盘落库 | ✅ | `SELECT * FROM annual_reviews;` |

---

## 📊 核心进度计算逻辑验证

### 关键结果(KR)进度公式
```
KR进度 = (数值进度 × 0.5 + 里程碑进度 × 0.3 + 任务进度 × 0.2) / 权重和
```

- **数值进度**: current_value / target_value × 100
- **里程碑进度**: 已完成里程碑数 / 总里程碑数 × 100
- **任务进度**: 已完成任务数 / 总任务数 × 100

### 目标进度公式
```
目标进度 = Σ(KR进度 × KR权重) / Σ(KR权重)
```

### 自动状态更新
- 进度 = 0% → `pending` (待开始)
- 0% < 进度 < 100% → `in_progress` (进行中)
- 进度 ≥ 100% → `completed` (已完成)

---

## 🔐 安全特性

1. **JWT认证**: 所有业务接口需要Bearer Token
2. **密码加密**: bcryptjs 哈希存储
3. **用户隔离**: 只能访问自己的数据
4. **操作审计**: 所有核心操作写入 operation_logs
5. **权限控制**: 管理员/普通用户角色分离
6. **CORS限制**: 仅允许配置的前端地址访问

---

## 📝 默认账号

| 账号 | 密码 | 角色 | 说明 |
|------|------|------|------|
| admin | admin123 | 管理员 | 可访问管理后台 |
| demo | user123 | 普通用户 | 预置5个年度目标 |

---

## 🐛 问题排查

### 常见问题

1. **前端页面空白**
   - 检查控制台是否有 SyntaxError
   - 确认 Vite 端口与 .env 一致
   - 查看 frontend.log 错误信息

2. **后端500错误**
   - 查看 backend.log 堆栈信息
   - 确认 SQLite 数据库文件存在且可写
   - 检查数据库表是否完整

3. **API请求失败**
   - 确认后端服务在运行
   - 检查 token 是否过期
   - 确认 CORS 配置正确

4. **端口被占用**
   - 使用 `lsof -nP -iTCP:端口 -sTCP:LISTEN` 检查
   - 确认 PID 归属后再决定是否终止
   - 服务会自动尝试备用端口

### 日志位置
- 前端日志: `frontend.log`
- 后端日志: `backend.log`
- 数据库: `data/app.sqlite`

export enum OrganizationType {
  headquarters = '总部',
  branch = '分公司',
  department = '部门',
  team = '小组',
}

export enum StoreType {
  gas_station = '气站',
  service_center = '服务中心',
  retail_store = '零售门店',
  warehouse = '仓库',
}

export enum Gender {
  male = '男',
  female = '女',
  unknown = '未知',
}

export enum ModuleType {
  parent = '父模块',
  child = '子模块',
}

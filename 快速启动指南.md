# 餐饮点餐收银系统 - 快速启动指南

## 一、环境准备

### 1.1 安装依赖
```bash
# Node.js 版本要求 >= 18.0.0
# MySQL 版本要求 >= 8.0
# Redis 版本要求 >= 6.0 (可选，用于缓存和消息队列)
```

### 1.2 安装 Node.js 依赖
```bash
# 进入项目根目录
cd xm-7119

# 使用 npm 安装依赖
npm install

# 或者使用 pnpm (推荐)
# pnpm install
```

---

## 二、数据库配置

### 2.1 创建数据库
```bash
# 登录 MySQL
mysql -u root -p

# 执行初始化脚本
source database/init.sql

# 或者手动创建数据库
CREATE DATABASE restaurant_pos DEFAULT CHARACTER SET utf8mb4;
```

### 2.2 创建数据库用户
```sql
CREATE USER 'restaurant_user'@'localhost' IDENTIFIED BY 'RestaurantPass2024';
GRANT ALL PRIVILEGES ON restaurant_pos.* TO 'restaurant_user'@'localhost';
FLUSH PRIVILEGES;
```

---

## 三、环境变量配置

配置文件位于项目根目录 `.env`，根据实际情况修改：

```env
# 端口配置
APP_PORT=3800          # 后端API端口
PROXY_PORT=3801        # 前端代理端口
ADMIN_PORT=3802        # 管理端开发端口
CUSTOMER_PORT=3803     # 顾客端开发端口

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=restaurant_user
DB_PASSWORD=RestaurantPass2024
DB_DATABASE=restaurant_pos

# Redis配置 (可选)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT配置
JWT_SECRET=restaurant_jwt_secret_key_2024_secure_abcdefg
JWT_EXPIRES_IN=7d

# 打印配置
PRINTER_TYPE=network
KITCHEN_PRINTER_IP=192.168.1.100
KITCHEN_PRINTER_PORT=9100
CASHIER_PRINTER_IP=192.168.1.101
CASHIER_PRINTER_PORT=9100
```

---

## 四、启动服务

### 4.1 开发模式启动

```bash
# 方式一：一次性启动所有服务 (推荐)
npm run dev

# 方式二：分别启动
# 启动后端
npm run dev:backend

# 启动管理端
npm run dev:admin

# 启动顾客端
npm run dev:customer
```

### 4.2 访问地址

启动成功后，访问以下地址：

| 服务 | 地址 | 说明 |
|------|------|------|
| 后端API | http://localhost:3800 | API服务 |
| API文档 | http://localhost:3800/api/docs | Swagger文档 |
| 管理端 | http://localhost:3802 | 店长/收银/厨师/服务员 |
| 顾客端 | http://localhost:3803 | 扫码点餐 |

---

## 五、默认账号

首次启动时，系统会自动创建默认用户账号：

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 系统管理员 | admin | Admin@123 | 全权限 |
| 店长 | manager | Manager@123 | 报表、员工管理 |
| 收银员 | cashier | Cashier@123 | 收银、会员管理 |
| 服务员 | waiter | Waiter@123 | 桌台、订单管理 |
| 厨师 | chef | Chef@123 | 后厨制作、出餐 |

**重要提示：首次登录后请立即修改默认密码！**

---

## 六、功能验证流程

### 6.1 顾客扫码点餐流程
1. 打开顾客端 http://localhost:3803
2. 点击"桌号 A01"快速体验
3. 浏览菜单，添加菜品到购物车
4. 点击购物车，确认订单
5. 提交订单

### 6.2 后厨接单流程
1. 打开管理端 http://localhost:3802
2. 使用厨师账号 chef / Chef@123 登录
3. 进入"后厨工作台"查看新订单
4. 点击"开始制作"确认接单
5. 逐个菜品标记"已出餐"

### 6.3 收银结账流程
1. 使用收银员账号 cashier / Cashier@123 登录
2. 进入"收银台"查看待支付订单
3. 选择支付方式（现金/微信/支付宝）
4. 确认支付，完成订单

---

## 七、生产部署

### 7.1 构建项目
```bash
# 构建所有项目
npm run build

# 分别构建
npm run build:backend
npm run build:admin
npm run build:customer
```

### 7.2 Nginx 配置示例

```nginx
# /etc/nginx/conf.d/restaurant.conf

# 主服务
upstream backend {
    server 127.0.0.1:3800;
}

# 管理端静态资源
upstream admin {
    server 127.0.0.1:3802;
}

# 顾客端静态资源
upstream customer {
    server 127.0.0.1:3803;
}

server {
    listen 80;
    server_name your-domain.com;

    # API代理
    location /api {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # WebSocket代理
    location /ws {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 管理端 (admin.your-domain.com)
    location /admin {
        proxy_pass http://admin;
        try_files $uri $uri/ /index.html;
    }

    # 顾客端 (your-domain.com 或 scan.your-domain.com)
    location / {
        proxy_pass http://customer;
        try_files $uri $uri/ /index.html;
    }
}
```

### 7.3 PM2 进程管理

```bash
# 安装 PM2
npm install -g pm2

# 启动后端服务
cd backend
pm2 start dist/main.js --name "restaurant-backend"

# 保存进程列表
pm2 save

# 设置开机自启
pm2 startup
```

---

## 八、常见问题

### Q1: 数据库连接失败
**解决方法：**
1. 检查 MySQL 服务是否启动
2. 检查 `.env` 中的数据库配置
3. 确认数据库用户权限

### Q2: 打印功能无法使用
**解决方法：**
1. 开发环境下使用模拟模式，不会实际打印
2. 生产环境检查打印机IP配置
3. 确认打印机与服务器网络连通

### Q3: WebSocket 连接失败
**解决方法：**
1. 检查后端服务是否正常启动
2. 检查 Nginx 代理配置（Upgrade 头）
3. 确认防火墙端口开放

---

## 九、技术支持

如遇问题，请检查：
1. Node.js 版本 >= 18.0.0
2. MySQL 版本 >= 8.0
3. 所有依赖是否正确安装
4. 环境变量配置是否正确

---

**版本：** v1.0  
**最后更新：** 2026-04-28

# 线下门店收银对账系统 - 测试样例

## 项目信息
- 项目目录: `/Users/chen/Documents/trae_projects/local_projects/may-63420`
- 前端端口: 43420 (SLOT 0)
- 后端端口: 53420 (SLOT 0)
- 备用槽位: 41000/51000, 42000/52000, 43000/53000, 44000/54000, 45000/55000 + 3420

---

## 一、启动说明

### 1.1 启动服务
```bash
cd /Users/chen/Documents/trae_projects/local_projects/may-63420
./scripts/start.sh
```

### 1.2 检查状态
```bash
./scripts/status.sh
```

### 1.3 停止服务
```bash
./scripts/stop.sh
```

### 1.4 主要文件
| 文件路径 | 说明 |
|---------|------|
| `.env` | 环境配置，端口配置 |
| `vite.config.ts` | Vite配置，strictPort、代理 |
| `backend/src/index.ts` | 后端入口，端口绑定 |
| `backend/src/db.ts` | 数据库初始化，表结构 |
| `src/App.tsx` | 路由配置 |
| `scripts/start.sh` | 启动脚本 |
| `scripts/stop.sh` | 停止脚本 |
| `scripts/status.sh` | 状态检查脚本 |

---

## 二、测试账号

所有测试账号密码统一为: `123456`

| 用户名 | 角色 | 权限 |
|-------|------|------|
| `admin` | 系统管理员 | 全部权限 |
| `finance01` | 财务 | 对账、报表、退款审核 |
| `manager01` | 门店店长 | 门店管理、交班复核、退款审核 |
| `cashier01` | 收银员 | 收银、订单、交班 |
| `cashier02` | 收银员 | 收银、订单、交班 |
| `area01` | 区域运营 | 查看报表、门店数据 |

---

## 三、测试样例分类

### 🟢 第一类：正常样例

#### 测试用例 1.1: 用户登录与权限验证
**测试路径**: `/login` → `/`
**步骤**:
1. 打开 `http://127.0.0.1:43420/login`
2. 输入用户名 `cashier1`，密码 `123456`
3. 点击登录
4. 验证跳转到首页，显示收银员工作台
5. 验证侧边栏菜单：收银台、订单管理、交班管理
6. 登出，使用 `admin` 登录
7. 验证侧边栏菜单显示全部功能

**预期结果**:
- 登录成功，token 存储在 localStorage
- 不同角色看到不同的侧边栏菜单
- 无权限页面访问会被重定向到首页

---

#### 测试用例 1.2: 收银台创建订单
**测试路径**: `/pos`
**前置条件**: 已开始交班
**步骤**:
1. 点击「开始交班」按钮
2. 在商品列表中点击添加几个商品到购物车
3. 点击「选择会员」，输入手机号 `13800138001` 搜索
4. 选择该会员，验证会员折扣显示
5. 点击「选择优惠」，选择一个可用优惠
6. 输入发票信息（可选）
7. 选择支付方式：现金，输入实收金额 100
8. 点击「确认收款」
9. 验证订单创建成功，显示订单号和找零

**预期结果**:
- 订单创建成功，返回200
- 订单号格式: `POS{YYYYMMDD}{6位序号}`
- 订单流水包含 hash 校验值
- 库存扣减正确
- 会员积分增加
- 支付记录生成

---

#### 测试用例 1.3: 交班管理
**测试路径**: `/shifts`
**步骤**:
1. 收银员登录，进入交班管理
2. 点击「开始交班」（如未开始）
3. 创建几笔测试订单（不同支付方式）
4. 点击「结束交班」
5. 录入各支付方式实收金额：
   - 现金: 500
   - 扫码: 300
   - 银行卡: 200
   - 储值卡: 100
   - 优惠券: 50
6. 验证系统自动计算应收、实收、差额
7. 点击「确认交班」

**预期结果**:
- 交班记录生成，状态为已交班
- 应收金额 = 所有订单金额按支付方式汇总
- 差额 = 实收 - 应收
- 非零差额标记为长款（绿色）或短款（红色）

---

#### 测试用例 1.4: 退款申请与审核
**测试路径**: `/refunds`
**步骤**:
1. 收银员登录，进入订单管理
2. 找到一笔已支付订单，点击「申请退款」
3. 选择退款原因：商品质量问题
4. 输入退款金额（不超过订单金额）
5. 提交退款申请
6. 使用店长账号 `manager1` 登录
7. 进入退款管理，找到待审核退款单
8. 点击「审核」，选择「通过」或「驳回」

**预期结果**:
- 退款申请创建成功，状态为待审核
- 退款金额 > 100元时，提示需要店长复核
- 银行卡支付必须原路退回
- 审核通过后订单状态更新为已退款
- 审核驳回后退款单状态为已驳回

---

#### 测试用例 1.5: 财务对账
**测试路径**: `/reconciliation`
**步骤**:
1. 财务账号 `finance` 登录
2. 点击「新建对账」
3. 选择门店、日期范围、收银员
4. 点击「开始对账」
5. 系统自动比对订单、支付、退款明细
6. 查看对账详情，标记差异项
7. 确认对账完成

**预期结果**:
- 对账记录生成，包含所有明细
- 每笔订单标记为：一致、长款、短款
- 对账完成率统计正确
- 支持导出对账报告

---

#### 测试用例 1.6: 数据报表与导出
**测试路径**: `/reports`
**步骤**:
1. 管理员或财务登录
2. 进入数据报表页面
3. 查看「数据概览」标签页：销售额、订单数、客单价、退款率
4. 查看「销售趋势」标签页：近7天销售柱状图
5. 查看「门店统计」、「收银员统计」等其他标签页
6. 点击「导出日结单」，选择日期范围
7. 验证 CSV 文件下载

**预期结果**:
- 所有统计数据正确显示
- 柱状图正确展示销售趋势
- 导出的 CSV 文件包含完整日结数据
- 异常订单列表正确显示

---

### 🟡 第二类：边界样例

#### 测试用例 2.1: 订单金额为0（全优惠券支付）
**测试路径**: `/pos`
**步骤**:
1. 选择一个商品，金额 50元
2. 选择一张 50元 优惠券
3. 验证应付金额为 0
4. 选择支付方式：优惠券
5. 确认收款

**预期结果**:
- 订单创建成功，金额为0
- 支付记录生成，金额为0
- 不产生实际支付流水

---

#### 测试用例 2.2: 部分退款
**测试路径**: `/refunds`
**步骤**:
1. 创建一笔 200元 订单
2. 申请部分退款 80元
3. 店长审核通过
4. 验证订单状态为部分退款
5. 再次申请剩余 120元 退款

**预期结果**:
- 部分退款成功
- 订单状态正确更新
- 累计退款金额不超过订单金额
- 第二次退款成功后订单状态为已退款

---

#### 测试用例 2.3: 交班零差额
**测试路径**: `/shifts`
**步骤**:
1. 开始交班
2. 创建订单：现金支付 100元
3. 结束交班，录入现金实收 100元
4. 验证差额为 0

**预期结果**:
- 差额显示为 0.00，灰色显示
- 交班状态正常

---

#### 测试用例 2.4: 多门店数据隔离
**测试路径**: 各页面
**步骤**:
1. 使用 `cashier1`（门店1）创建订单
2. 使用 `cashier2`（门店2）创建订单
3. 使用 `manager1`（门店1店长）登录
4. 验证只能看到门店1的订单
5. 使用 `admin` 登录
6. 验证可以看到所有门店数据

**预期结果**:
- 门店用户只能看到所属门店数据
- 管理员可以看到所有门店数据
- 数据按门店正确隔离

---

### 🔴 第三类：冲突样例

#### 测试用例 3.1: 重复退款申请
**测试路径**: `/refunds`
**步骤**:
1. 找到一笔已全额退款的订单
2. 尝试再次申请退款

**预期结果**:
- 系统提示「该订单已全额退款，无法再次申请」
- 退款申请被拒绝

---

#### 测试用例 3.2: 库存不足下单
**测试路径**: `/pos`
**步骤**:
1. 选择库存为 0 的商品
2. 尝试添加到购物车

**预期结果**:
- 系统提示「库存不足」
- 无法添加到购物车
- 或在确认收款时提示库存不足，订单创建失败

---

#### 测试用例 3.3: 端口占用冲突
**测试步骤**:
1. 手动占用 43420 端口（使用其他程序）
2. 运行 `./scripts/start.sh`
3. 观察脚本行为

**预期结果**:
- 脚本检测到端口被占用
- 检查占用进程是否属于当前项目
- 如果不属于，自动切换到备用槽位（44420/54420）
- 更新 .env 文件中的端口配置
- 使用新端口启动服务

---

#### 测试用例 3.4: 无权限访问
**测试路径**: 各受限页面
**步骤**:
1. 收银员登录
2. 手动输入 URL 访问 `/admin`
3. 手动输入 URL 访问 `/reconciliation`

**预期结果**:
- 自动重定向到首页
- 显示无权限提示
- 侧边栏不显示无权限菜单

---

### ⚫ 第四类：失败样例

#### 测试用例 4.1: 错误密码登录
**测试路径**: `/login`
**步骤**:
1. 输入正确用户名 `cashier1`
2. 输入错误密码 `wrongpassword`
3. 点击登录

**预期结果**:
- 显示「用户名或密码错误」提示
- 不生成 token
- 不跳转到首页

---

#### 测试用例 4.2: 创建订单支付金额不足
**测试路径**: `/pos`
**步骤**:
1. 选择商品，应付金额 100元
2. 选择现金支付，录入实收 50元
3. 点击确认收款

**预期结果**:
- 系统提示「实收金额不足」
- 订单创建失败
- 需要补充实收金额或更换支付方式

---

#### 测试用例 4.3: 订单验真失败（篡改hash）
**测试路径**: `/orders`
**步骤**:
1. 找到一笔订单，记录订单号和 hash
2. 调用订单验真 API，传入错误的 hash
3. 或直接修改数据库中的订单金额

**预期结果**:
- 验真失败，提示「订单数据可能被篡改」
- 订单详情页显示「数据异常」标记
- 操作日志记录验真失败事件

---

#### 测试用例 4.4: 未开始交班创建订单
**测试路径**: `/pos`
**步骤**:
1. 收银员登录，未开始交班
2. 尝试添加商品并创建订单

**预期结果**:
- 系统提示「请先开始交班」
- 无法创建订单
- 引导用户先开始交班

---

## 四、导出核对

### 4.1 日结单导出
**接口**: `GET /api/reports/daily-export?date=YYYY-MM-DD`
**验证点**:
1. CSV 文件编码为 UTF-8
2. 包含字段：订单号、门店、收银员、金额、支付方式、状态、创建时间
3. 金额格式正确，保留2位小数
4. 数据与当天订单一致

### 4.2 交班单导出
**接口**: `GET /api/shifts/:id/export`
**验证点**:
1. 包含交班时段、各支付方式应收/实收/差额
2. 包含订单笔数统计
3. 包含收银员和店长签字栏

### 4.3 对账报告导出
**接口**: `GET /api/reconciliation/:id/export`
**验证点**:
1. 包含对账范围、统计汇总
2. 包含每笔订单的核对状态
3. 差异项高亮标记

---

## 五、API 测试样例

### 5.1 健康检查
```bash
curl -s http://127.0.0.1:53420/api/health | jq
```
**预期**: 返回 code=0, data.status="ok"

### 5.2 登录获取 Token
```bash
curl -s -X POST http://127.0.0.1:53420/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"cashier1","password":"123456"}' | jq
```
**预期**: 返回 code=0, data.token="..."

### 5.3 获取订单列表
```bash
TOKEN="your_token_here"
curl -s -H "Authorization: Bearer $TOKEN" \
  http://127.0.0.1:53420/api/orders?page=1&pageSize=10 | jq
```
**预期**: 返回订单列表，包含分页信息

### 5.4 创建订单
```bash
TOKEN="your_token_here"
curl -s -X POST http://127.0.0.1:53420/api/orders \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "items": [{"productId": 1, "quantity": 2, "price": 29.9}],
    "payments": [{"method": "cash", "amount": 59.8, "received": 100}],
    "memberId": 1,
    "invoice": {"need": false}
  }' | jq
```

---

## 六、进程隔离验证

### 6.1 验证只杀当前项目进程
```bash
# 1. 启动其他项目的服务占用 43420 端口
# 2. 运行 ./scripts/start.sh
# 3. 验证：
#    - 不 kill 其他项目进程
#    - 自动切换到备用端口
#    - 输出占用进程信息但不终止
```

### 6.2 固定命令验证
```bash
PROJECT_DIR="$(pwd)"
FRONTEND_PORT=43420
BACKEND_PORT=53420

frontend_pid=$(lsof -nP -iTCP:$FRONTEND_PORT -sTCP:LISTEN -t | head -n1)
backend_pid=$(lsof -nP -iTCP:$BACKEND_PORT -sTCP:LISTEN -t | head -n1)

ps -p "$frontend_pid" -o pid=,ppid=,stat=,cwd=,command=
ps -p "$backend_pid" -o pid=,ppid=,stat=,cwd=,command=

curl -I --max-time 5 http://127.0.0.1:$FRONTEND_PORT/
curl -sS --max-time 5 http://127.0.0.1:$BACKEND_PORT/api/health
```

---

## 七、热更新验证

### 7.1 前端热更新
1. 修改 `src/pages/Home.tsx` 中的文案
2. 保存文件
3. 观察浏览器是否自动刷新
4. 验证不需要重启整个项目

### 7.2 后端热更新
1. 修改 `backend/src/routes/orders.ts` 中的逻辑
2. 保存文件
3. 观察后端日志，确认 tsx watch 自动重启
4. 验证 API 调用返回更新后的结果

---

## 八、验收标准

✅ 所有服务启动成功，端口监听正常  
✅ 前后端 HTTP 200 响应正常  
✅ 核心业务链路可完整走通  
✅ 所有测试用例通过  
✅ 进程隔离约束遵守  
✅ 端口约束遵守  
✅ 日志文件正确写入  
✅ 导出功能正常  

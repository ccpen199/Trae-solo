# 直播带货系统 - 启动说明

## 系统概述

本系统是一个完整的直播带货解决方案，包含以下核心组件：

- **后端服务** (Node.js + Express + SQLite + Redis)
  - 端口: 9876
  - 功能: API 服务、WebSocket 实时通信、秒杀引擎、库存引擎、时间线记录

- **前端应用** (React + Vite + Ant Design)
  - 端口: 9878
  - 功能: 直播观看、弹幕互动、商品购买、秒杀抢购、订单管理

## 端口策略

### 为什么选择这些端口？

为了避免与常见开发端口（3000、5173、8080、4200、3001 等）冲突，我们选择了以下稀有端口：

| 服务 | 端口 | 用途 | 优先级 |
|------|------|------|--------|
| 后端 API | 9876 | REST API + WebSocket | 高 |
| 前端应用 | 9878 | Web 应用 | 高 |
| Redis | 6379 | 缓存/秒杀队列 | 中（可选） |

### 端口冲突处理

**重要：** 启动前请确保以下端口未被占用。

#### 检查端口是否被占用

**Windows 系统：**
```powershell
# 检查 9876 端口
netstat -ano | findstr :9876

# 检查 9878 端口  
netstat -ano | findstr :9878

# 检查 6379 端口
netstat -ano | findstr :6379
```

**Linux/Mac 系统：**
```bash
# 检查 9876 端口
lsof -i :9876

# 检查 9878 端口
lsof -i :9878
```

#### 如果端口被占用

**方法一：关闭占用端口的进程（推荐）**

Windows:
```powershell
# 找到占用进程的 PID
netstat -ano | findstr :9876

# 结束进程（替换 PID 为实际数字）
taskkill /PID <PID> /F
```

Linux/Mac:
```bash
# 找到占用进程的 PID
lsof -i :9876

# 结束进程（替换 PID 为实际数字）
kill -9 <PID>
```

**方法二：修改配置文件中的端口**

如果无法关闭占用进程，可以修改配置文件：

**后端端口修改：**
编辑 `backend/.env` 文件：
```env
PORT=9877  # 修改为其他可用端口
```

**前端端口修改：**
编辑 `frontend/vite.config.js` 文件：
```javascript
server: {
  port: 9879,  // 修改为其他可用端口
  proxy: {
    '/api': {
      target: 'http://localhost:9877',  // 与后端端口保持一致
      changeOrigin: true
    }
  }
}
```

**可用端口建议：**
9876, 9877, 9878, 9879, 9880, 9881, 9882

## 环境要求

### 必需软件

1. **Node.js** (版本 >= 18.0.0)
   - 下载地址: https://nodejs.org/
   - 检查版本: `node -v`

2. **Redis** (可选，用于高性能秒杀)
   - 下载地址: https://redis.io/download
   - 如果没有 Redis，系统会自动使用内存模式（适合开发测试）
   - 检查版本: `redis-cli --version`

### 可选软件

1. **Git** (用于代码版本管理)
2. **VS Code** (推荐 IDE)

## 快速启动

### 方式一：一键启动（推荐）

```bash
# 1. 安装全局依赖
npm install

# 2. 安装前后端依赖
npm run install:all

# 3. 启动开发环境
npm run dev
```

### 方式二：分别启动

```bash
# 1. 安装依赖
cd backend
npm install

cd ../frontend
npm install

# 2. 启动后端（终端 1）
cd backend
npm run dev

# 3. 启动前端（终端 2）
cd frontend
npm run dev
```

### 生产环境启动

```bash
# 1. 构建前端
cd frontend
npm run build

# 2. 启动后端
cd backend
npm start

# 3. 预览前端
cd frontend
npm run preview
```

## 访问地址

启动成功后，请访问以下地址：

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端应用 | http://localhost:9878 | 主入口 |
| 后端 API | http://localhost:9876 | API 服务 |
| 健康检查 | http://localhost:9876/api/health | 检查后端状态 |

## 默认账号

系统初始化时会创建以下演示账号：

| 角色 | 用户名 | 密码 | 权限说明 |
|------|--------|------|----------|
| 平台管理员 | admin | admin123 | 全部权限 |
| 主播 | streamer1 | streamer123 | 开启直播、发起秒杀 |
| 主播 | streamer2 | streamer123 | 开启直播、发起秒杀 |
| 商家 | merchant1 | merchant123 | 管理商品、处理订单 |
| 商家 | merchant2 | merchant123 | 管理商品、处理订单 |
| 观众 | viewer1 | viewer123 | 观看直播、购买商品 |
| 观众 | viewer2~5 | viewer123 | 观看直播、购买商品 |

## 核心功能演示流程

### 场景一：主播开启直播并发起秒杀

1. **主播登录** (streamer1 / streamer123)
2. **创建直播间**
   - 点击左侧菜单"直播间"
   - 点击"创建直播间"按钮
   - 输入标题，可选选择商品
   - 点击"创建"
3. **开始直播**
   - 在"我的直播"标签页
   - 点击"开始直播"按钮
4. **发起秒杀**
   - 进入直播间
   - 在商品橱窗点击"发起秒杀"
   - 设置秒杀价格（建议低于原价）
   - 设置秒杀库存
   - 点击"确认发起"
5. **结束直播**
   - 点击"结束直播"按钮
   - 系统自动生成直播统计

### 场景二：观众观看直播并抢购

1. **观众登录** (viewer1 / viewer123)
2. **进入直播间**
   - 点击左侧菜单"直播间"
   - 选择正在直播的房间
   - 点击卡片进入
3. **互动功能**
   - 发送弹幕聊天
   - 点击点赞按钮
4. **商品购买**
   - 在右侧商品橱窗选择商品
   - 点击"立即购买"
5. **秒杀抢购**
   - 当主播发起秒杀时，会收到弹窗通知
   - 点击"立即抢购"
   - 确认购买
6. **订单管理**
   - 点击左侧菜单"我的订单"
   - 查看订单状态
   - 待支付订单可点击"支付"
   - 已发货订单可点击"确认收货"

### 场景三：商家管理商品和订单

1. **商家登录** (merchant1 / merchant123)
2. **商品管理**
   - 点击左侧菜单"商品管理"
   - 点击"添加商品"
   - 填写商品信息（名称、价格、库存等）
   - 点击"创建"
3. **库存管理**
   - 点击"补货"按钮
   - 输入补充数量
   - 点击"确认"
4. **订单处理**
   - 点击左侧菜单"我的订单"
   - 查看订单状态
   - 待发货订单可点击"发货"

### 场景四：平台管理员查看数据和日志

1. **管理员登录** (admin / admin123)
2. **查看运营数据**
   - 点击左侧菜单"运营后台"
   - 在"数据概览"查看整体统计
3. **查看直播列表**
   - 切换到"直播列表"标签
   - 查看所有直播场次
4. **查看热销商品**
   - 切换到"热销商品"标签
   - 可按时间范围筛选
5. **查看主播排行**
   - 切换到"主播排行"标签
   - 查看主播业绩排名
6. **查看秒杀活动**
   - 切换到"秒杀活动"标签
   - 查看所有秒杀记录
7. **查看审计日志**
   - 切换到"审计日志"标签
   - 查看所有操作记录
   - 可按时间范围筛选

## 系统架构说明

### 后端架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Express Server                          │
│                         (Port 9876)                           │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Routes    │  │  Socket.IO  │  │    Middleware       │  │
│  │  (API路由)  │  │ (实时通信)  │  │  (认证/错误处理)    │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐    │
│  │                    Engines (核心引擎)                │    │
│  ├─────────────┬─────────────┬───────────┬─────────────┤    │
│  │  FlashSale  │  Inventory  │  Live     │  Timeline   │    │
│  │  (秒杀引擎) │ (库存引擎)  │(互动引擎) │ (时间线引擎) │    │
│  └─────────────┴─────────────┴───────────┴─────────────┘    │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐    │
│  │                    Data Layer                         │    │
│  ├─────────────────────┬─────────────────────────────────┤    │
│  │      SQLite3        │            Redis                 │    │
│  │   (关系型数据库)    │      (缓存/消息队列)            │    │
│  └─────────────────────┴─────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 权限架构

系统采用基于角色的访问控制 (RBAC)：

```
┌──────────────────────────────────────────────────────────────┐
│                        platform_admin                           │
│                    (平台管理员 - 全部权限)                      │
├──────────────────────────────────────────────────────────────┤
│    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐ │
│    │   streamer   │    │   merchant   │    │    viewer    │ │
│    │    (主播)    │    │    (商家)    │    │    (观众)    │ │
│    ├──────────────┤    ├──────────────┤    ├──────────────┤ │
│    │ 开启直播     │    │ 管理商品     │    │ 观看直播     │ │
│    │ 结束直播     │    │ 处理订单     │    │ 购买商品     │ │
│    │ 发起秒杀     │    │ 库存管理     │    │ 发送弹幕     │ │
│    │ 查看统计     │    │ 查看报表     │    │ 管理订单     │ │
│    └──────────────┘    └──────────────┘    └──────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### 实时数据流转

```
┌─────────────┐     WebSocket      ┌─────────────┐
│   主播端    │◄─────────────────►│   观众端    │
│  (streamer) │                    │  (viewer)   │
└──────┬──────┘                    └──────┬──────┘
       │                                  │
       │    弹幕消息 / 点赞 / 礼物         │
       ▼                                  ▼
┌─────────────────────────────────────────────────────┐
│              Socket.IO Server (Port 9876)           │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐    │
│  │           LiveInteractionEngine               │    │
│  │         (实时互动处理引擎)                     │    │
│  └─────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │  弹幕广播   │  │  点赞计数   │  │  秒杀通知   │ │
│  └─────────────┘  └─────────────┘  └─────────────┘ │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐    │
│  │           TimelineEngine (时间线记录)         │    │
│  │      所有操作都会被记录到时间线，可追溯         │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

## 故障排查

### 常见问题

**Q1: 启动时提示端口被占用**

```
Error: listen EADDRINUSE: address already in use :::9876
```

解决方法：
1. 按照"端口冲突处理"章节中的方法关闭占用进程
2. 或者修改配置文件中的端口号

**Q2: 无法连接到 Redis**

解决方法：
1. 检查 Redis 是否启动：`redis-cli ping`
2. 如果没有 Redis，可以忽略，系统会自动使用内存模式
3. 内存模式适合开发测试，但不建议用于生产环境

**Q3: 前端页面无法访问后端 API**

解决方法：
1. 检查后端是否正常启动
2. 访问 http://localhost:9876/api/health 检查健康状态
3. 检查前端 vite.config.js 中的代理配置是否正确

**Q4: WebSocket 连接失败**

解决方法：
1. 检查后端是否正常启动
2. 检查 token 是否有效（需要先登录）
3. 查看浏览器控制台的错误信息

**Q5: 秒杀功能不工作**

解决方法：
1. 确保直播间状态为"直播中"
2. 确保商品有足够库存
3. 检查主播权限（只有主播可以发起秒杀）
4. 如果使用 Redis，确保 Redis 正常运行

### 日志查看

系统会自动记录日志到以下位置：

- 后端日志: `backend/logs/`
  - `error.log` - 错误日志
  - `combined.log` - 综合日志

- 数据库文件: `backend/data/live_stream.db`

## 性能优化建议

### 开发环境

1. 使用 Node.js 内置的开发模式即可满足需求
2. Redis 可选，但建议安装以测试完整功能
3. 可以使用 concurrently 同时启动前后端

### 生产环境

1. **使用 PM2 管理进程**
   ```bash
   npm install -g pm2
   pm2 start backend/src/index.js --name "live-stream-backend"
   ```

2. **使用 Nginx 反向代理**
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;

       location / {
           proxy_pass http://localhost:9878;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }

       location /api {
           proxy_pass http://localhost:9876;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }

       location /socket.io {
           proxy_pass http://localhost:9876;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

3. **数据库优化**
   - 考虑使用 MySQL/PostgreSQL 替代 SQLite
   - 添加数据库连接池
   - 添加适当的索引

4. **缓存策略**
   - 使用 Redis 作为缓存层
   - 配置 Redis 集群
   - 添加 CDN 加速静态资源

## 技术栈详情

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | >= 18.0.0 | 运行环境 |
| Express | 4.18.2 | Web 框架 |
| Socket.IO | 4.7.2 | 实时通信 |
| SQLite3 | 5.1.6 | 关系型数据库 |
| ioredis | 5.3.2 | Redis 客户端 |
| JWT | 9.0.2 | 身份认证 |
| bcryptjs | 2.4.3 | 密码加密 |
| Winston | 3.10.0 | 日志记录 |
| Jest | 29.7.0 | 测试框架 |

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.2.0 | UI 框架 |
| Vite | 4.4.9 | 构建工具 |
| Ant Design | 5.9.0 | UI 组件库 |
| React Router | 6.15.0 | 路由管理 |
| Zustand | 4.4.1 | 状态管理 |
| Axios | 1.5.0 | HTTP 客户端 |
| Socket.IO Client | 4.7.2 | WebSocket 客户端 |
| Tailwind CSS | 3.3.3 | CSS 框架 |
| Day.js | 1.11.9 | 日期处理 |

## 联系方式

如有问题或建议，请联系技术支持。

---

**版本信息**
- 系统版本: 1.0.0
- 更新日期: 2026-04-29
- Node.js 要求: >= 18.0.0

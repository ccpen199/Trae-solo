export enum PatternStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  SUBMITTED = 'submitted',
  CONFIRMED = 'confirmed',
  REJECTED = 'rejected',
  NEEDS_REVISION = 'needs_revision',
  ARCHIVED = 'archived',
}

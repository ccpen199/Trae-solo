spring:
  application:
    name: coldchain-logistics
  
  datasource:
    url: jdbc:sqlite:/Users/chen/Documents/trae_projects/local_projects/xm-7012/data/app-dev.sqlite
    username: 
    password: 
    driver-class-name: org.sqlite.JDBC
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLiteDialect
        format_sql: true

server:
  port: 7012
  servlet:
    context-path: /api

jwt:
  secret: coldchain_secret_key_dev_2024
  expiration: 86400

logging:
  level:
    com.coldchain: debug
    org.hibernate.SQL: debug

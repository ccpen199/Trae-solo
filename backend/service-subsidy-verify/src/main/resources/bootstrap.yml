spring:
  application:
    name: service-subsidy-verify
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: guizhou-platform
        group: DEFAULT_GROUP
      config:
        server-addr: 127.0.0.1:8848
        namespace: guizhou-platform
        group: DEFAULT_GROUP
        file-extension: yaml
        shared-configs:
          - data-id: common.yaml
            group: DEFAULT_GROUP
            refresh: true

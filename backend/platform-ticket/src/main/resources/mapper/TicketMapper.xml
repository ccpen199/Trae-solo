<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guizhou.platform.ticket.mapper.TicketMapper">

    <select id="avgProcessHours" resultType="java.lang.Double">
        SELECT AVG(TIMESTAMPDIFF(HOUR, create_time, complete_time))
        FROM ticket
        WHERE deleted = 0 AND complete_time IS NOT NULL AND create_time >= #{startTime}
    </select>

</mapper>

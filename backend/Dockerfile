# 项目根目录环境变量
COMPOSE_PROJECT_NAME=community-forum

# 数据库配置
POSTGRES_PORT=15432
POSTGRES_USER=community_user
POSTGRES_PASSWORD=CommunityPass123!
POSTGRES_DB=community_db

# Redis配置
REDIS_PORT=16379
REDIS_PASSWORD=RedisPass123!

# 后端配置
BACKEND_PORT=18763
DATABASE_URL=postgresql://community_user:CommunityPass123!@localhost:15432/community_db
REDIS_URL=redis://:RedisPass123!@localhost:16379

# 前端配置
FRONTEND_PORT=28763
VITE_API_URL=http://localhost:18763
